---
import MainLayout from "../layouts/MainLayout.astro";
import Button from '../components/Button.astro';
import landingImg from '../assets/images/landing_billede.png';
---
<MainLayout title="Titel">
  <section class="w-full">
    <!-- Hero sektion -->
    <section class="relative w-full">
      <!-- Billede -->
      <img
        src={landingImg.src}
        alt="Indgangsparti i Nykredit Huset"
        class="w-full object-cover"
      />

      <!-- Overlejret tekst -->
      <div class="absolute inset-0 flex items-center justify-center text-center pointer-events-none">
        <div class="text-lysgul-500 text-subheading-02-mobile sm:text-heading-01-desktop font-regular leading-tight">
          <div class="flex flex-wrap justify-center gap-1">
            {Array.from('BYGNINGSINTEGRERET KUNST').map((char) => (
              <span class="inline-block pointer-events-auto translate-y-0 transition-transform duration-300 ease-out hover:-translate-y-4">
                {char === ' ' ? '\u00A0' : char}
              </span>
            ))}
          </div>
          <div class="flex flex-wrap justify-center gap-1 mt-s">
            {Array.from('I NYKREDIT HUSET').map((char) => (
              <span class="inline-block pointer-events-auto translate-y-0 transition-transform duration-300 ease-out hover:-translate-y-4">
                {char === ' ' ? '\u00A0' : char}
              </span>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Tekst og knap -->
    <div class="flex flex-col items-end justify-center gap-3xl px-l md:px-6xl py-6xl text-black max-w-screen-xl mx-auto">
      <p class="text-body-xl-mobile sm:text-body-xl-desktop">
        De bygningsintegrerede værker er nøje udvalgt og udviklet i samspil med arkitekturen for at skabe rum, der inspirerer, engagerer og åbner for nye perspektiver. Kunstens rolle er ikke kun æstetisk, men skal også styrke følelsen af tilhør og gøre hverdagen rigere for både medarbejdere og besøgende.
        Oplev hvordan kunst og arkitektur går i dialog og se værkerne med egne øjne på en rundvisning.
      </p>

      <Button label="Book en rundvisning" />
    </div>
  </section>
</MainLayout>
